const businessResolvers = {
  Query: {
    getBusinessList: async (_, __, { user }) => {
      return "TODO";
    },
  },
  Mutation: {
    addBusiness: async (_, input, { user }) => {
      return "TODO";
    },
    updateBusiness: async (_, input, { user }) => {
      return "TODO";
    },
  },
};

export default businessResolvers;
